LocalLeaf uses a local Node host service, browser-based editing, and a session model that stops when the host stops.

The host machine owns the files, runs compilation, manages approvals, and controls when the session ends.
